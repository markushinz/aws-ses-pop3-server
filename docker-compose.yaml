version: "3.7"
services:
  aws-ses-pop3-server-service:
    build: .
    # image: markushinz/aws-ses-pop3-server:latest
    restart: always
    ports:
      - 110:110
      - 995:995
    volumes:
      - ./config.yaml:/etc/aws-ses-pop3-server/config.yaml
